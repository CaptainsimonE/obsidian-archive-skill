---
name: obsidian-archive
description: 把用户提供的内容（本轮对话、指定对话片段、粘贴的文本或笔记）完整归类、格式化并归档到 Obsidian 库（PARA 体系）的 skill。当用户明确要求归档时使用，不限内容类型——如「归档本次对话」「把这段内容整理进 Obsidian」「总结归档」「存进知识库」「放进 Obsidian」「记下来」。skill 负责全部分类与规范化：知识问答整理为知识笔记，项目内容整理为项目笔记，日常内容追加到当日日记，无法归类的内容放入 Inbox；产出遵循库内既有规范（中文扁平标签、YAML frontmatter、库内正文风格、双链关联、MOC 维护）。
agent_created: true
---

# Obsidian Archive（对话与内容归档）

将用户放进来的内容（对话、文本、笔记）完整归类、格式化、规范化，沉淀进 Obsidian 库。用户只负责提供内容，分类与排版由本 skill 完成。

## 配置（CONFIG）

> 部署时请将 `<VAULT_ROOT>` 替换为你自己的 Obsidian 库根目录绝对路径。

- Vault 根目录：`<VAULT_ROOT>`
- 知识归档目录（30-Resources）：`<VAULT_ROOT>/30-Resources`
- 项目目录（10-Projects）：`<VAULT_ROOT>/10-Projects`
- 年度记录目录（20-Areas）：`<VAULT_ROOT>/20-Areas/2026年度记录`
- Inbox 目录（00-Inbox）：`<VAULT_ROOT>/00-Inbox`
- 模板：本 skill 的 `references/templates.md`

**关键陷阱：库内目录名均带描述后缀，路径必须使用完整名称**（如 `30-Resources--兴趣、知识库，扁平化 + MOC`），缩写会找不到路径。

## 何时触发

1. 用户明确要求归档，**不限内容类型**：知识问答、项目讨论、日常杂谈、粘贴的素材均可。
2. 输入形态有三种：
   - **本次对话**：用户说「归档本次对话/刚才聊的内容」，归档当前会话完整内容；
   - **指定片段**：用户指出要归档的对话段落或单条问答；
   - **粘贴内容**：用户直接粘贴文本/笔记/素材，要求整理进 Obsidian。
3. 归档对象不明确时（如对话包含多个无关主题），先确认归档范围再执行。

## 工作流

### 1. 确定归档对象
按用户指令确定内容范围：整轮对话 / 指定问答 / 粘贴文本。范围含糊时先澄清。

### 2. 总结提炼
对内容做总结提炼，产出**有持久价值的结构化摘要**：
- 保留：核心结论、关键事实与数字、方法步骤、代码/命令、决策理由、待办事项；
- 丢弃：寒暄客套、过程性噪音、重复表达；
- 保真：不歪曲原意、不编造内容、不擅自补充外部知识；回答不完整时如实记录已有部分。

### 3. 归类决策（PARA 决策树）
按内容性质判定落点（见下方决策树），并匹配主题。判定后不反复询问，**skill 替用户完成归类**；仅在多种归类均合理且影响较大时，给出推荐并询问。

### 4. 查重
- 目标文件夹内检索相关既有笔记（文件名 + frontmatter 关键词）。
- 高度重复：报告用户，三选一——新建 / 合并进已有笔记 / 跳过，未确认前不写入。
- 相关但内容不同：正常新建，并在笔记内用 `[[双链]]` 关联。

### 5. 格式化
严格按 `references/templates.md` 中对应类型的模板生成，遵守「规范化铁律」。

### 6. 写入
- 用 Write 工具以绝对路径写入（中文/emoji 路径已验证可用），UTF-8 编码。
- 若 Write 报路径错误，改用 Python 一行脚本兜底：`open(path, 'w', encoding='utf-8')`。

### 7. 双链与 MOC
- 正文中涉及库内既有概念/笔记时用 `[[笔记名]]` 内链。
- 知识笔记写入 30-Resources 后，若目标主题文件夹存在 MOC（`-NN-主题MOC.md`），把 `[[新笔记名]]` 追加进其 `## 双链区`，并为笔记 frontmatter 添加 `up: "[[该MOC]]"`。
- **不自动创建 MOC**（用户刻意策展）；**不修改 `.base` 数据库文件**；**不删改既有笔记**（日记追加除外，见模板四）。

### 8. 报告
回复中给出：归档文件绝对路径、归类依据（PARA 类别 + 主题文件夹）、标签列表；跳过/合并了重复项时说明原因；提示可随时要求调整。

## 归类决策树

| 内容性质 | 落点 | 模板 |
|---------|------|------|
| 知识性内容（概念/原理/方法/教程/事实/对比） | 30-Resources 主题子文件夹（无匹配则新建中文主题文件夹） | 一、知识笔记 |
| 多主题对话的总结性归档 | 30-Resources 主题文件夹（取主主题），或 00-Inbox（主题分散时） | 二、对话总结 |
| 明确围绕某项目（项目名/任务/进度） | 10-Projects 对应项目文件夹（如 `202606-网页游戏开发`） | 三、项目进展 |
| 日常流水、感想、杂谈 | 20-Areas 年度记录当日日记追加 | 四、日记追加 |
| 临时素材、主题不明、待定内容 | 00-Inbox（提示用户定期整理，符合库内「先记录后整理」原则） | 二、对话总结 |

主题匹配方式：运行时用 Glob/遍历列出 30-Resources 现有子文件夹（如 `知识管理`、`🖥️服务器知识`、`📚读书笔记`、`音乐`、`生活技巧`），按内容主主题匹配，**不要凭记忆猜目录名**。

## 规范化铁律

- **文件名**：中文短语式主题名；无日期前缀、无 `-NN-` 序号（MOC 专用）；不含 `？ ? / \ : * " < > |`；重名加限定词。
- **frontmatter**：`title` / `created`（YYYY-MM-DD）/ `tags` / `source: "WorkBuddy 问答归档"`；`up` 仅在主题文件夹已有 MOC 时添加。
- **标签**：中文扁平标签 2-4 个（1 主题 + 1-3 关键词），不用层级标签；优先复用库内已有标签（见 templates.md）。
- **正文**：知识笔记与对话总结不写 H1（库内风格，标题靠文件名承载）；日记沿用其自带 H1 模板。
- **双链**：`[[笔记名]]` 用于关联库内既有笔记；新笔记之间不要强凑链接。
- **编码与路径**：UTF-8；目录名带 `--描述` 后缀须用全名。
